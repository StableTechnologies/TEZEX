import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  root: {
    "& .MuiDialog-paper": {
      textAlign: "center",
      borderRadius: "24px",
    },
  },

  completed: {
    width: "160px",
    width: "160px",
  },

}));

